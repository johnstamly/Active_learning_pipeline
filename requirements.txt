# Versions are the ones used to produce the published results.
# Newer minor versions should work; for full reproducibility, pin exactly.

torch==2.1.0
gpytorch>=1.11,<1.13
numpy==1.24.3
pandas==1.5.3
scipy==1.9.3
scikit-learn==1.3.0
matplotlib==3.7.2
plotly==5.9.0
joblib==1.2.0
PyYAML==6.0.1
openpyxl==3.0.10

# Notebook execution
jupyter>=1.0
ipykernel>=6.0
